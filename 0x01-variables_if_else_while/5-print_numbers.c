#include <stdio.h>
/**
 * main - Entry Point
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int base10;

	for (lower_case = 'a'; lower_case <= 'z'; lower_case++)
	{
		prinf(%base10, base10);
	}
	printf('\n');
	return (0);
}

